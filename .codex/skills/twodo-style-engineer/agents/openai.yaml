interface:
  display_name: "TwoDo 樣式工程師"
  short_description: "為 TwoDo 實作專案限定的 CSS、SCSS 與 Tailwind，避免不必要的全域樣式擴散。"
  default_prompt: "Use $twodo-style-engineer 在這個 TwoDo 專案中實作或重構樣式，使用專案限定的 CSS、SCSS 與 Tailwind 模式，並避免新增不必要的全域樣式。"

policy:
  allow_implicit_invocation: true
